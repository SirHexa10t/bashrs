Reimplement the bashrc commands in my other repo

Copy over the TODOs

Add a print at the end of sourcefile, notifying the user if his personal-data (inserted by user manually) indicates that something's coming up:
    subscription renewal (service, credit-card benefits, infrastructure provider's discount...)
    birthdays
    deadlines
    appointments
    

youtube/ffmpeg:
    scraping historic snapshots of channels to find if any of their videos have been de-listed/disappeared

    Add dl_alt (downloading/accessing through online services)

synonym finder

karaoke function (mix of `dl` and `media_mute_human_voice`)

Add-in filemanager's conf files
    applies during installation; initial choice in conf/templates/configrs.toml for the inclusion of these files: false (off).
    Explain that it supports Nemo(?) specifically
        Maybe broaden the support range?
    Maybe also include browser extensions - they need to be copied into OS drive (not hyperlinked!(?)) in order to hold between resets

Similarly, add-in plugins-management (saving to HOME so that the browser won't forget them)?


Add Spotify to `dl`

taking inventory of games-library


USB-cable speed-tester 
    diagnosis for:
        full-functionality (no inner tears)
        transfer-speed (and concluded USB version)
    should take into account the limit on the ports which were used (is this easy to do?)
    should also cover usb-drives? (make sure to fully flood the cache?) ; or maybe just recommend a program that does that.
    can it be done by connecting 2 of the computer's ports with the same cable?

android de-bloating


dl:
    add subtitle-scrubbing function? (or is it attached in a standard way and can be put in bashrs's `media`?)
    test if .mkv is truly more compact, verify if it's really less lossy (are we converting to it post-download?)
    download from twitter
        support downloading images
        entire accounts' history, including txt files (post, reception(likes/retweets), community notes), videos/gifs, images
    


jukebox:
    <dir-of-playable-media> (default: .)
    -r (recursively include all media files)
    --formats <comma-separated-extensions> (overwrite default formats list; default [print bashrs's known formats])
    --round-trip (use random-that-randomly-visits-each-file-once rather than totally-random)
    --dirs <comma-separated-strings> (overwrite single-dir scan; jukebox on more than one location) <- too complicated?

    Each time a new track is played, prefix that line with timestamp (up to seconds)
    Start run by specifying the used seed (assuming that's how we'd determine the random path through tracks)


network:
    discover local network (devices (detail) ; addresses ; other metadata (active? answering on some ports? uptime?))


git:
    bundle git + git-lfs in the program's offered libraries (like Python3)
    install huggingface ?


files:
    save open window-manager paths (and locations across screens if possible)
    get bookmarks from nemo, offer `bookmark_cd` function (add "_<num>" for duplicate entries)
    `gui` function (error message if running headless)
    mountpoints command + configuration file




anti-ai:
list of unusual characters, such as '—'
    go look for a list of ai-affiliated words (like "delve"))
    replace words with synonyms, at a high "temperature" (consideration of lower-likeness words) setting
        scrubbing the technique mentioned here: https://www.youtube.com/watch?v=3FhxdhVMJoU
            the method for creating the "watermarks" is in accordance with the algorithm at github: google-deepmind/synthid-text

at some point, move out the document formatting code into Carl (non-Marks).

alias cat command (show line numbers, maybe color text)

Calendaric notifications could go into comfygurations
    maybe forms would need to include list-editing


Get a rusty htop, or implement own:
    CPU graph, MEM graph, top-CPU processes, top-MEM processes (all at once).
        Network graph?
        GPU graph?
        Do we need a view by parent process? (are sub-processes displayed at the top, or do their parents appear?)
            Might need to dsiplay with indentations, to show the tree structure you'd be taking down
        Make sure that crucial details are visible, for distinguishing between similar processes (time started, full command-line (args), PID, parent PID (...?))

`pixel_paint`
    probably in a project of its own
ricing module
    `ricing_backup` would save aside the dot-files and files from /etc/


bluetooth in net_local

