
use std::env;
use bashrs::commands::Command;

fn main() {
    let binary = env::current_exe()
        .expect("Could not resolve current binary path")
        .parent()
        .expect("Could not resolve binary directory")
        .join("bashrs");

    println!("# Auto-generated by generate_sourceable — do not edit manually");
    println!("# Re-run install.sh to update\n");

    for cmd in Command::ALL {
        println!(
            "function {name} () {{ '{bin}' '{name}' \"$@\"; }}",
            name = cmd.name(),
            bin  = binary.display(),
        );
    }
}