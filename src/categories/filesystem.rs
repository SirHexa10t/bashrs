#[bashrs_macros::category(command = FilesystemCommand, prefix = "fs_")]
mod commands {
    // Filesystem commands live here — each `pub fn` becomes an `fs_*` command.
    // Intentionally empty for now; the category is kept ready to extend.
}
